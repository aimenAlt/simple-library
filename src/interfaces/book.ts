export default interface Book {
  title: string; //"title"
  authors: string[]; //"author_name"
  firstPublishedYear: number; //"first_publish_year"
  isbn: string[]; // "isbn"
  numberOfPages: number; //"number_of_pages_median"
}
